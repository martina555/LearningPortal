@{ 
    Layout = "/Views/Shared/_Layout.cshtml";
}

<h2 style="padding-top: 20px">Manage your account</h2>

<div>
    <h4>Change your account settings</h4>
    <hr />
    <div class="row"> @*bylo 3a9*@
        <div class="col-md-2">
            @await Html.PartialAsync("_ManageNav")
        </div>
        <div class="col-md-10">
            @RenderBody()
        </div>
    </div>
</div>

@section Scripts {
    @RenderSection("Scripts", required: false)
}
        
